@AGENTS.md

<!--
  This file exists so Claude Code finds the working agreements, and contains
  nothing of its own.

  AGENTS.md is the open standard — Codex, Cursor, Copilot, Gemini CLI, Aider,
  Windsurf and Zed read it at the repository root without being told. Claude
  Code still looks for CLAUDE.md, so this points at the other file with an
  import rather than repeating it.

  Copying the content here instead would give the repository two sets of
  working agreements that agree today and drift by next month, and the one that
  drifted would still be read by half the tools. If your agent reads neither
  filename, add its own with the same one-line import — never a second copy.
-->
